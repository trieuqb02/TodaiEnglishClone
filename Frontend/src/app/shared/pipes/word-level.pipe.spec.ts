import { WordLevelPipe } from './word-level.pipe';

describe('WordLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new WordLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
